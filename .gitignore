# Visual Studio user and workspace files
.vs/
*.user

# Build output
bin/
obj/

# コードの行数を数える拡張機能の出力
.VSCodeCounter

# codex
.codex/

# github-copilot
.github/

# Python バイトコードキャッシュ
__pycache__/

# ランタイム同梱 MSI（55MB）と wixpdb はリポジトリに入れず、Releases の添付だけで配る
# Build/release artifacts (published via GitHub Releases)
publish/

# Claude Code のローカル専用設定（共有しない）
.claude/settings.local.json
.claude/settings.json
.codex-loop/

# 個人のエージェント運用ツール（このアプリの実装とは無関係なので共有しない）
.claude/skills/codex-loop/
.claude/codex-loop-tools/
.claude/codex-loop.json
.mcp.json

# graphify: 再生成できる生成物・キャッシュ・自動バックアップは追跡しない
# （graph.json と GRAPH_REPORT.md だけあれば query / explain は動く）
graphify-out/cache/
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
graphify-out/memory/
graphify-out/graph.html
.claude/settings.json.graphify-bak

graphify-out/.graphify_python
graphify-out/.graphify_root

# AIエージェント用の一時フォルダ
.tmp/
