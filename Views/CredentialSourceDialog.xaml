<Window x:Class="JpScratch.Views.CredentialSourceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Gemini API キー"
        Width="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        WindowStyle="None"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        Background="{DynamicResource WindowBackgroundBrush}">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32"
                            ResizeBorderThickness="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{DynamicResource ChromeBackgroundBrush}">
                <TextBlock Text="Gemini API キー"
                           Margin="12,0,0,0"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="{DynamicResource SubtleTextBrush}" />
                <Button HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,2,0"
                        Style="{StaticResource ChromeButton}"
                        FontFamily="Segoe MDL2 Assets"
                        Content="&#xE8BB;"
                        ToolTip="閉じる"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Click="UseStoredButton_Click" />
            </Grid>

            <StackPanel Grid.Row="1" Margin="20,18,20,18">
                <TextBlock Text="環境変数 GEMINI_API_KEY が見つかりました。こちらを使用しますか？"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextBrush}"
                           FontSize="13" />
                <TextBlock x:Name="StoredKeyStatusText"
                           Margin="0,10,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource SubtleTextBrush}"
                           FontSize="11" />
                <TextBlock Text="この選択は記憶され、設定画面からいつでも変更できます。キーの値は画面やログへ表示しません。"
                           Margin="0,6,0,16"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource SubtleTextBrush}"
                           FontSize="11" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource AppButton}"
                            Content="アプリに保存したキーを使う"
                            MinWidth="180"
                            IsCancel="True"
                            Click="UseStoredButton_Click" />
                    <Button Style="{StaticResource AppButton}"
                            Content="環境変数を使う"
                            MinWidth="140"
                            Margin="8,0,0,0"
                            IsDefault="True"
                            Click="UseEnvironmentButton_Click" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
