# 要件定義書 — 常駐型 日本語スクラッチパッド（仮称: JP Scratch）

- 版: 1.1
- 作成日: 2026-07-28
- 最終更新日: 2026-07-31
- 対象プラットフォーム: Windows 11 (x64)

---

## 1. 背景と目的

### 1.1 解決したい問題

| # | 課題 | 現状の回避策と、その不満 |
|---|---|---|
| P-1 | WSL や一部アプリで日本語（IME）が入力できない | 別途 Notepad++ を起動している。フルスクリーンで開いたり、ウィンドウが変な位置に出たりして、マウス移動のコストが高い |
| P-2 | 日本語入力時に誤字・脱字・変換ミス・タイポが発生する | 目視で直すしかない |
| P-3 | LLM に修正させると文章の癖まで矯正されてしまう | 例: ラフな文章の「ら」抜き言葉を勝手に直される。使い続けるほど自分の文体から離れる |

### 1.2 目的

1. **どこからでも 1 アクションで呼び出せて、すぐ消える** 日本語入力用の常駐エディタを提供し、P-1 を解消する。
2. Gemini を用いた誤字脱字チェックを **1 箇所ずつ承認/拒否できる粒度** で提供し、P-2 を解消する。
3. 拒否のリアクションを蓄積・学習し、**ユーザーの文体を尊重する校正** を実現して P-3 を解消する。
4. トークン消費を可視化し、費用が読めない不安を取り除く。

### 1.3 設計原則

- **常駐アプリとしての礼儀を守る**: メモリを食わない、起動が速い、邪魔にならない。
- **文体は資産である**: 校正は「明らかな誤り」に限定し、迷ったら提案しない。提案の分類より、余計な修正を出さないことを優先する。
- **費用は常に見える**: 何にいくらかかったかを隠さない。

---

## 2. 技術スタック

| 項目 | 選定 | 理由 |
|---|---|---|
| 言語 / ランタイム | C# / .NET 10 (LTS) | Windows ネイティブ、AOT でなくとも ReadyToRun で起動が速い |
| UI フレームワーク | WPF | 日本語 IME の挙動（変換中の未確定文字列、候補ウィンドウ位置）が最も確実 |
| エディタコンポーネント | AvalonEdit (ICSharpCode.AvalonEdit 6.x) | 行番号・折り返し・検索置換パネル・装飾層（下線描画）・`TextAnchor`（編集に追従する位置マーカー）が標準装備 |
| DB | SQLite (Microsoft.Data.Sqlite) | 履歴・課金ログ・学習データ用。単一ファイル、依存なし |
| HTTP | `HttpClient` + `System.Text.Json` | 追加依存なし |
| 配布 | MSI インストーラー（WiX v5/v6） | スタートアップ登録・ショートカット作成まで面倒を見る |

### 2.1 非機能要件

| 項目 | 目標値 |
|---|---|
| 常駐時メモリ (Working Set) | 80 MB 以下（アイドル時） |
| コールドスタート（トレイ常駐開始まで） | 1.5 秒以下 |
| ホットキー押下 → 入力可能まで | 100 ms 以下（プロセスは常駐済みでウィンドウ表示のみ） |
| 校正リクエストの往復 | 2 秒以内を目標（Flash Lite クラス想定） |
| インストーラーサイズ | 100 MB 以下 |

> ウィンドウは初回起動時に生成して非表示のまま保持し、以降は `Show()` / `Hide()` で切り替える。毎回 `new Window()` すると 100 ms 目標を達成できない。

---

## 3. 機能要件

### 3.1 常駐とウィンドウ制御

#### 3.1.1 タスクトレイ

- タスクトレイに常駐する。左クリックでウィンドウをトグル表示。
- 右クリックメニュー: `表示` / `設定` / `課金履歴` / `終了`。
- アイコンで状態を示す（通常 / 校正リクエスト中 / API エラー / 月間上限到達）。
  **未実装**（`Services/TrayIconService.cs` は単一アイコンのみで、状態による切り替えを持たない）。
  月間上限ガード（3.6.3）実装時にバルーン通知は追加したが、アイコン自体の出し分けはスコープ外にした。
  v2 の独立チェック項目として切り出す（5. マイルストーンの v2 チェックリスト参照）。

#### 3.1.2 表示位置とサイズ

設定で以下 3 方式を切り替える。**既定はタスクバーのあるモニタの右下**。

| 方式 | 挙動 |
|---|---|
| タスクバー右下（既定） | トレイアイコンのあるモニタの作業領域右下に吸着 |
| マウスカーソルのあるモニタの右下 | ホットキー起動時に、今作業しているモニタに出す |
| 前回位置を復元 | 一度置いた位置を維持。初回のみ右下 |

- ウィンドウサイズはユーザーがリサイズした値を記憶する。既定は 480 × 600 px。
- モニタ構成の変更・DPI 変更時に画面外へ出ないよう、表示前に必ず作業領域内へクランプする。
- 常に手前に表示（Topmost）は設定で ON/OFF、既定 ON。

#### 3.1.3 自動非表示と、隠すときの挙動

設定項目として以下 2 つを独立に持つ。

| 設定 | 既定 | 説明 |
|---|---|---|
| フォーカス喪失時に自動的に隠す | ON | 他アプリをクリックしたらトレイに引っ込む |
| 隠すときに本文をクリップボードにコピー | OFF | 隠す直前にアクティブタブの全文（または選択範囲があればそれ）をクリップボードへ入れる |

> クリップボードへのコピーは **すべての非表示経路** で効く。フォーカス喪失だけでなく、
> 表示トグルのホットキー・`Esc`・タイトルバーの閉じるボタンも対象。隠れたあとで
> 「コピーし忘れた」と気づいても取り返せないので、隠し方で挙動を変えない。

- タイトルバーに **ピン留めボタン** を置く。ピン中は自動非表示を一時的に無効化する（設定を書き換えない）。
- 自動非表示 ON でも、設定ダイアログ・検索パネル・IME 候補ウィンドウにフォーカスがある間は隠さない（IME 変換中の消失は致命的）。

#### 3.1.4 グローバルホットキー

すべて設定画面から再割り当て可能。競合時は登録失敗を検知してユーザーに通知する。

| 機能 | 既定の割当 | スコープ |
|---|---|---|
| 表示 / 非表示トグル | `Alt + Space` | グローバル |
| 全文をコピーして隠す | `Ctrl + Alt + Enter` | グローバル |
| 校正を今すぐ実行 | `Ctrl + Enter` | ウィンドウ内 |
| 次の提案へ移動 | `F8` | ウィンドウ内 |
| 前の提案へ移動 | `Shift + F8` | ウィンドウ内 |
| 選択中の提案を許可 | `Ctrl + .` | ウィンドウ内 |
| 選択中の提案を拒否 | `Ctrl + ,` | ウィンドウ内 |

> 「全文をコピーして隠す」は、隠した後に **直前のフォアグラウンドウィンドウへフォーカスを戻す**。WSL のターミナルに戻って `Ctrl+Shift+V` で貼るまでが 1 キー + 1 キーで完結する。

---

### 3.2 エディタ

#### 3.2.1 タブ

- 複数タブ。ドラッグで並べ替え、中クリックで閉じる、`Ctrl+T` 新規 / `Ctrl+W` 閉じる / `Ctrl+Tab` 切替。
- タブ名は本文 1 行目から自動生成（最大 20 文字）。ダブルクリックで手動リネーム可。
- タブ構成と本文は再起動で完全復元される（3.2.4 参照）。
- 閉じたタブは 30 日間ゴミ箱に保持し、`Ctrl+Shift+T` で復元できる。

#### 3.2.2 編集機能

- 行番号表示、現在行ハイライト、折り返し ON/OFF、全角スペース・タブ・改行の可視化（ON/OFF）。
- 無制限 Undo / Redo。
- 文字数・行数・選択文字数をステータスバーに表示（**日本語文書では文字数が実用上重要**）。
- フォント既定は `Meiryo UI` → `游ゴシック` → `Yu Gothic UI` のフォールバック。`Ctrl + マウスホイール` でサイズ変更、設定でフォント・サイズを固定可。
- テーマは **OS 追従（ライト/ダーク）＋ 手動固定**。

#### 3.2.3 検索・置換

Notepad++ 相当。

- 通常検索 / 大文字小文字を区別 / 単語単位 / **正規表現**（.NET 正規表現）。
- 置換 / すべて置換 / 選択範囲内のみ置換。
- ヒット件数の表示と、本文中の全ヒット箇所のハイライト。
- **全タブ横断検索**: 開いているタブとゴミ箱を含む全スクラッチを対象に検索し、結果一覧からジャンプできる。

#### 3.2.4 永続化（スクラッチパッド型）

- 「保存」という概念をユーザーに見せない。編集は**自動保存**される。
- 保存タイミング: 入力停止 1 秒後（デバウンス）、タブ切替時、ウィンドウ非表示時、アプリ終了時。
- 保存先: `%APPDATA%\JpScratch\tabs\{tab_id}.txt`（UTF-8, BOM なし, CRLF）。
- **プレーンテキストで保存する理由**: アプリが壊れてもメモ帳でサルベージできること自体が要件。
- 書き込みは一時ファイルに保存してから `File.Replace` で置き換え、書き込み中のクラッシュでも既存の本文を失わない。
- 任意で `.txt` へのエクスポート（`Ctrl+Shift+S`）を提供する。インポートは行わない（メモ帳型の「開く」は非対応）。

---

### 3.3 校正機能

#### 3.3.1 実行トリガー

- **入力停止後の自動チェック（デバウンス方式）**。
- 発火条件（すべて満たしたときのみ送信）:
  1. 最後のキー入力から `N` 秒経過（既定 2 秒、設定可）
  2. 前回送信から `M` 秒以上経過（既定 10 秒、設定可）— **最小送信間隔の強制**
  3. 対象段落の内容ハッシュが前回送信時と異なる
  4. IME 変換が確定済み（未確定文字列がない）
  5. 月間上限に達していない（実装済み。`Services/UsageLimitService.cs`、`Views/MainWindow.xaml.cs`。実機確認済み）
- `Ctrl + Enter` でデバウンスを待たず即時実行できる。
- 自動チェックは設定で OFF にできる（OFF 時は手動のみ）。

#### 3.3.2 送信範囲

- 単位は **段落**（空行で区切られたブロック。空行がない場合は改行単位）。
- 送信内容 = **変更のあった段落（校正対象）＋ その前後 1 段落（文脈、修正対象外と明示）**。
- 選択範囲がある状態で手動実行した場合は、選択範囲を校正対象とする。
- 対象段落が 2,000 文字を超える場合は分割して逐次送信する。

#### 3.3.3 校正対象

以下を校正対象とする。ただし、全文修正版からローカル差分を作る方式では個々の変更を
正確に分類できないため、**v2 ではカテゴリ別の ON/OFF と `confidence` 閾値を設けない**。
提案ごとのカテゴリはモデル・アプリのどちらでも生成せず、UIにも表示しない。

| カテゴリ | 既定 | 例 |
|---|---|---|
| 誤字 | ON | コミニュケーション → コミュニケーション |
| 脱字 | ON | 美容につて → 美容について |
| 変換ミス | ON | 防腐剤を問ふ → 防腐剤を塗布 |
| タイポ（打鍵ミス・かな崩れ） | ON | キーぼーぢ → キーボード |
| 文法エラー（助詞の誤用・重複） | ON | この文章ア → この文章が |
| 表記ゆれ | ON | 同一文書内の「サーバ / サーバー」混在 |
| 句読点 | ON | 読点の欠落・過剰 |
| 言い回し・読みやすさの改善 | **対象外（v1〜v3 で実装しない）** | — |

**文体保護の絶対ルール**（プロンプトにハードコードし、ユーザー設定でも解除できない）:

- 「ら」抜き言葉・「い」抜き言葉を誤りとして扱わない
- 口語表現・くだけた語尾・体言止め・倒置を修正しない
- 敬体/常体の混在をそれ自体としては修正しない
- 語彙をより「上品な」ものに置き換えない
- **意図した表現である可能性が少しでもあるなら、提案しない**

#### 3.3.4 提案の表示

**インライン下線 ＋ 下部パネルの併用**方式。

```
┌─────────────────────────────────────────┐
│ [タブ1] [タブ2] [+]                      │
├─────────────────────────────────────────┤
│  1 │ この文章ア、間違いが二ともある。     │
│    │      ~~~~~~        ~~~~~~           │  ← 波線
│  2 │                                     │
├─────────────────────────────────────────┤
│ ⚠ 2/2                                    │  ← 下部パネル（提案があるときだけ表示）
│ 「文章ア」→「文章が」                     │
│ [許可(Ctrl+.)] [拒否(Ctrl+,)] [理由をつけて…▾] │
├─────────────────────────────────────────┤
│ 12行 340文字 │ 直近 $0.000021 (¥0.003)   │  ← ステータスバー
└─────────────────────────────────────────┘
```

- **一文中の複数箇所は、それぞれ独立した提案として扱う**。上記の例なら「文章ア→文章が」と「二とも→二つとも」で別々にリアクションできる。
- `F8` / `Shift+F8` で提案を順送りし、キーボードだけで処理を完了できる。
- 下線をクリックしても、その提案が下部パネルに選択される。
- 提案が 0 件のときは下部パネルを畳み、エディタの高さを最大化する。
- 全文方式ではモデルから提案ごとのカテゴリ・理由を受け取らない。固定値にも意味がないため、
  提案モデルには保持せず、UIにも表示しない。分類・理由生成の追加 API 呼び出しも行わない。

#### 3.3.5 提案位置の解決（重要な設計上の論点）

LLM に文字オフセットや個別置換を返させると、日本語（サロゲートペア・結合文字）や置換範囲の
取り方が安定しない。プロンプト単独検証の結果を受け、以下の方式を採る。

1. モデルには前後の文脈を含む文書全文を渡し、**明らかな誤りだけを直した文書全文**を返させる。
2. アプリ側で原文と修正版を Unicode の書記素単位で比較し、Myers 差分から互いに重ならない局所置換を作る。
3. 1語内で最大2書記素だけ離れた差分は1提案へまとめる。一文中でも離れた誤りは別提案のままにする。
4. 純粋な挿入（脱字修正）は隣接する1書記素を原文・修正案の両方へ含め、範囲を持つ置換に変換する。
5. 改行コード（CRLF / LF）の違いとモデルが付けた末尾改行だけは変更として扱わない。
6. 全提案を原文へ再適用し、修正版全文を完全に再現できることを検証する。重複、50件超、
   1箇所200書記素超、編集距離500超、または20書記素を超えて全文の20%超が変わる結果は
   **提案を一件も出さず応答全体を破棄する**。
7. 差分の書記素位置を UTF-16 オフセットへ変換し、AvalonEdit の `TextAnchor` と範囲アンカーで保持する。
   以降ユーザーが本文を編集しても位置が追従する。
8. **提案範囲そのものがユーザーに編集されたら、その提案は自動的に失効させる**（古い提案を適用して壊すのを防ぐ）。

#### 3.3.6 リアクション

| 操作 | 挙動 | 学習への影響 |
|---|---|---|
| **許可** | 本文を置換し、提案を消す | 「この修正は妥当」として蓄積 |
| **拒否** | 提案を消す（本文は変えない） | 「この修正は不要」として蓄積 |
| **理由をつけて退ける** | 理由を入力 → 提案を消す | 理由つきで蓄積。学習の主材料 |
| **理由をつけて別案を出す** | 理由を入力 → その箇所だけ再リクエスト | 蓄積 ＋ 追加の API 呼び出しが発生（費用が出ることを UI に明示） |

- 「理由をつけて…」はドロップダウンで上記 2 種を選べる。
- 理由の入力欄には、よく使う理由の履歴からサジェストを出す（例:「これは意図した口語表現です」）。
- Undo（`Ctrl+Z`）で本文は戻るが、蓄積されたリアクション記録は残す。誤操作の取り消しは履歴画面から行う。

---

### 3.4 学習機能（文体の適応）

3 つの仕組みを組み合わせる。

#### 3.4.1 リアクション履歴の few-shot 同梱

- 直近および関連度の高いリアクションを最大 `K` 件（既定 15 件）プロンプトに含める。
- 選定ロジック: (a) 拒否・理由つき拒否を優先、(b) 校正対象テキストと語句が重なるものを優先、(c) 新しいものを優先。
- 送信例のフォーマット:
  ```
  - 「文章ア」→「文章が」: 許可された
  - 「思ってた」→「思っていた」: 拒否された（理由: 話し言葉として意図的）
  ```

#### 3.4.2 スタイルガイドの自動生成

- リアクションが **50 件たまるごと**（設定可）に、蓄積された履歴を LLM に要約させ、ルール文（10〜20 行程度）を生成する。
- 生成されたスタイルガイドは**設定画面で全文が読め、ユーザーが自由に編集・削除できる**。ブラックボックス化させない。
- 生成の実行前に確認ダイアログを出す（費用が発生するため）。自動生成 OFF も可。
- 生成例:
  ```
  - 「ら」抜き言葉は意図的な口語表現。修正しない
  - 「〜かな」「〜だよね」などの語尾は保持する
  - 「サーバー」表記を好む（「サーバ」に統一しない）
  - 技術用語のカタカナ表記は原文を尊重する
  ```
- 履歴が増えるほど few-shot が肥大するのを、スタイルガイドへの圧縮で防ぐ（トークン効率の要）。

#### 3.4.3 ユーザー手書きのカスタム指示欄

- 設定画面に自由記述のテキストエリアを置き、その内容をプロンプト末尾に必ず付加する。
- 自動生成されたスタイルガイドより**優先度を高く**扱う（プロンプト上でより後ろ、かつ「最優先」と明示）。

#### 3.4.4 プロンプト構成（送信順）

```
1. システム指示（役割・出力形式・文体保護の絶対ルール）
2. 校正対象の範囲（明らかな誤字・脱字・変換ミス等）
3. 自動生成スタイルガイド（あれば）
4. ユーザー手書きカスタム指示（あれば・最優先と明示）
5. 過去のリアクション例（few-shot, 最大 K 件）
6. `<document>` 境界
7. 文脈を含む校正対象全文
```

> 1〜5 は変化が少ないため、Gemini のコンテキストキャッシュ（対応していれば）の適用対象として設計しておく。

---

### 3.5 API 連携

#### 3.5.1 Gemini API

- モデル: `gemini-3.5-flash-lite`（**確定。3.5.4 参照**）。設定画面でモデル ID を変更できる。
- `responseMimeType` は `text/plain`。文脈を含む校正対象全文を `<document>` 境界内で渡し、
  明らかな誤りだけを直した**文書全文だけ**を返させる。
- `<document>` 内はすべてデータであり、命令らしい文があっても従わないようシステム指示で固定する。
- 個別提案、位置、カテゴリ、理由、`confidence` はモデルへ生成させない。原文と修正版全文の差分から
  アプリ側で個別提案を生成する。カテゴリ・理由はアプリ側でも生成・表示・保存しない。

- Gemini 3 系の推奨に合わせ `temperature` は `1.0`。Gemini 3.5 Flash-Lite ではカスタム値が無視されるため、
  将来のモデル差し替え時に既定値から不用意に下げない。
- タイムアウト 15 秒。失敗時は 1 回だけ指数バックオフでリトライし、それ以上は再試行しない（自動チェックのリトライ地獄を避ける）。

#### 3.5.2 トークン数と料金

- **トークン数は推定せず、レスポンスの `usageMetadata` を使う**。入力は `promptTokenCount`、課金対象出力は `candidatesTokenCount + thoughtsTokenCount` とする。
- 単価は `pricing.json` に外出しする。設定画面（「校正（Gemini）」セクション「モデル単価」）からモデル別に
  入力単価・出力単価・更新日を編集できる。`pricing.json` の直接編集も引き続き可能。

```json
{
  "gemini-3.5-flash-lite": {
    "input_usd_per_1m": 0.30,
    "output_usd_per_1m": 2.50,
    "updated_at": "2026-07-29"
  }
}
```

- 料金 = `promptTokens / 1e6 × input単価 + billableOutputTokens / 1e6 × output単価`。`billableOutputTokens` は `candidatesTokenCount + thoughtsTokenCount`。
- 内部では `decimal` で保持し、丸めは表示時のみ行う。
- 表示: $ は小数点以下最大8桁（例 `$0.000021`）。¥ は通常小数第2位まで、`0 < |¥| < 0.01` は
  微小額を隠さないため小数第3位まで（例 `¥0.003`）。表示用レートは小数点以下最大4桁。永続ログ値は表示時まで丸めない。

#### 3.5.3 為替レート（Frankfurter API）

- エンドポイント: `https://api.frankfurter.dev/v2/rate/USD/JPY?providers=ECB`。v2の単一通貨ペア応答を使い、
  ECB providerを固定して旧 `frankfurter.app/latest` の汎用レスポンス依存をなくす。
- **1 日 1 回・ローカルキャッシュ**。`fetched_at` のローカル日付が当日でなければ、起動時または初回換算時に取得する。
  Frankfurter の `rate_date` は前営業日のままで正常とする。
- 成功・失敗を問わず取得試行のローカル日付を保存し、同じ日には再試行しない。ネットワーク失敗時は古いキャッシュを
  使い、キャッシュなしなら `¥—` のまま翌日まで待つ。
- Frankfurter は ECB 参照レートで**平日 1 日 1 回しか更新されない**。したがって:
  - 取得失敗時はキャッシュ値を使い続ける。
  - 直近ログでは `¥0.003@07-25` のようにレート基準日を併記する。期間合計は各ログに固定保存した
    円額を加算し、単一レートならそのレート・基準日、複数なら基準日範囲とレート件数をツールチップで示す。
  - キャッシュがまったくない状態で失敗した場合は `¥ —` と表示し、$ のみ表示する。
- 週末・祝日に「今日のレートがない」のは正常系として扱い、エラー表示しない。

#### 3.5.4 モデルの確認状況

**確認済み**（2026-07-28。ユーザーが Google AI Studio で実際に使用し、モデル仕様書 `gemini-3.5-flash-lite.md` を提供）:

| 項目 | 結果 |
|---|---|
| モデル ID の実在 | `gemini-3.5-flash-lite`（安定版 ID も同じ） |
| 入力トークン上限 | 1,048,576 |
| 出力トークン上限 | 65,536 |
| Structured Output（`responseSchema`） | サポート対象 |
| コンテキストキャッシュ | サポート対象 |
| 思考 / 関数呼び出し | サポート対象 |

> このモデル ID を疑って作業を止めないこと。ユーザーが使い勝手まで検証したうえで指定している。

**追加確認済み**（2026-07-29）:

- Developer API 標準単価は入力 `$0.30 / 1M tokens`、出力（thinking tokens を含む）
  `$2.50 / 1M tokens`。根拠は `gemini-3.5-flash-lite.md` を参照。

**未確認**:

- コンテキストキャッシュの最小トークン数。

#### 3.5.5 API キーの管理

- **既定は DPAPI（`ProtectedData`, `CurrentUser` スコープ）で暗号化し `%APPDATA%\JpScratch\credentials.dat` に保存**。
- **起動時に環境変数 `GEMINI_API_KEY` が設定されていることを検出したら、初回のみダイアログで確認する**:
  > 環境変数 `GEMINI_API_KEY` が見つかりました。こちらを使用しますか？
  > `[環境変数を使う]` `[アプリに保存したキーを使う]`
  この選択は記憶し、以降は尋ねない（設定画面から変更可）。
- キーはログ・エラーメッセージ・クラッシュダンプに絶対に出力しない。設定画面でも常にマスク表示する。

---

### 3.6 コスト表示

#### 3.6.1 表示箇所

| 粒度 | 表示場所 | 内容 |
|---|---|---|
| 直近リクエスト | ステータスバー下段（常時、実装済み） | 最後に挿入した有効ログの `↑312 ↓48 $0.000050 (¥0.008)` と固定済みレート基準日 |
| セッション累計 | ステータスバー下段・ツールチップ（実装済み） | 起動からのUSD/JPY合計。保存済み固定レートの単一/複数情報を併記し、JPY欠損があれば `¥—` |
| 当日累計 | ステータスバー下段・ツールチップ（実装済み） | ローカル日次のUSD/JPY合計。日付切替時に再集計・日次FX取得を開始。課金履歴画面ヘッダで期間・種別を絞った集計も見られる |
| 当月累計 | ステータスバー下段・ツールチップ（実装済み） | ローカル月次のUSD/JPY合計。固定レートの根拠をツールチップで示す。上限額進捗バーも実装済み（ダーク・ライト両テーマで実機確認済み） |
| リクエスト単位のログ | 課金履歴画面 一覧（実装済み。実画面検証済み） | 日時・種別・モデル・入出力トークン・$・¥・適用レート・所要時間・成否・提案/破棄件数 |

複数段落の実行途中で失敗した場合も、成功済み応答と使用量が既知の失敗応答については、それぞれの応答時に固定した
USD/JPYスナップショットを合算して結果を表示する。使用量が不明な失敗は既知分と明示的に区別する。

#### 3.6.2 課金履歴画面（実装済み。実機確認済み）

- 期間フィルタ（当日 / 当週 / 当月 / 全期間 / カスタム）。実装済み。既定は当月。
- トリガー種別（自動 / 手動 / 別案生成 / スタイルガイド生成）で絞り込み。実装済み。全種別OFFなら「該当なし」で一覧を空にする。
- 明細一覧に日時・種別・モデル・入出力トークン・USD/JPY・レート基準日・所要時間・成否・提案/破棄件数を表示する。実装済み。
- CSV エクスポート。**実装済み**（`Services/BillingCsvExporter.cs`）。現在の期間・種別フィルタに合致する明細を
  BOM 付き UTF-8・CRLF の RFC 4180 形式で書き出す。一覧の表示上限（2,000件）は適用せず全件を書く。
  USD/JPY/レートは表示書式で丸めず保存済みの `decimal` をそのまま出す（表計算ソフトで再集計するため）。
  モデル名・エラー文は外部由来の自由文字列なので、`= + - @` などで始まる場合に `'` を前置して
  CSV インジェクションを防ぐ。
- 保持期間は既定 12 か月（設定画面 `ApiLogRetentionMonths`、0 で無期限）。それ以前の月の明細は
  バックグラウンドで日次サマリ `api_call_daily` へ圧縮し、`api_calls` から削除する。**実装済み**
  （`Services/ApiLogRetention.cs`、`ApiCallRepository.Compact`、`MainWindow.CompactApiLogsInBackground`）。
  実行契機は**起動後・設定変更後・日付が変わったとき**の3つ。境界は月初のローカル 0 時に丸めるので、
  削除されるのは必ず「保持期間以上前」の明細に限られる（保持期間 1 か月・現在 2026-07 なら境界は
  2026-06-01 で、前月の明細は残る）。
  **期間合計は圧縮しても変わらない**。`GetUsageSummary` が `api_calls` と `api_call_daily` を合算する。
  失われるのは 1 件ごとの明細（時刻・所要時間・エラー文）だけで、その件数は
  `ApiCallUsageSummary.CompactedCalls` として画面ヘッダと CSV 出力時に明示する。
  `reactions.api_call_id` は `api_calls(id)` への外部キーなので、リアクション行は残したまま
  そのリンクだけを NULL にする（学習データは消さない）。

#### 3.6.3 課金ガード（実装済み。実機確認済み）

| ガード | 既定 | 挙動 |
|---|---|---|
| **月間上限額** | $2.00 | 到達したら**自動チェックを停止**。トレイアイコンで通知。手動実行は「上限を超えます」の確認ダイアログを挟んで実行可。翌月 1 日 00:00（ローカル時間）にリセット。**実装済み**（`Services/UsageLimitService.cs`、`Views/MainWindow.xaml.cs`、`Views/SettingsWindow.xaml`）。設定画面から `MonthlyLimitUsd` を編集でき、0 は無制限として扱う |
| 上限接近の警告 | 80% | ステータスバーの進捗バーを警告色に変える。**実装済み**（`Themes/Styles.xaml` の `UsageProgressBar`、`UsageLimitWarningRatio` を設定画面から編集可） |
| **最小送信間隔** | 10 秒 | 前回送信からこの秒数が経つまで自動チェックを発火させない |
| デバウンス | 2 秒 | 入力停止からこの秒数待つ |
| 段落ハッシュによる重複抑止 | 常時 | 内容が変わっていない段落は再送しない（実装上の必然。設定項目にはしない） |

進捗バーはステータスバー下段に常時表示し、上限が 0（無制限）のときは `Visibility="Collapsed"` で消える。
`ProgressBar` は既定テンプレート（Aero2）が内部固定色で描くため、`ComboBox` 等と同じくテンプレートごと
差し替えてある（`Themes/Styles.xaml`）。判定基準は**送信前の当月累計が上限以上かどうか**で、今回の送信が
上限を超える見込みかの事前見積りは行わない（出力トークン数が送信前には分からないため）。
手動実行は上限到達後もブロックせず、確認ダイアログに当月累計と上限額を具体額で示す。
トレイ通知は「年月＋上限額」単位で一度だけに抑え、月替りまたは上限額変更で再度通知できる
（`UsageLimitNotificationTracker`）。ダークテーマでの見え方、警告色・到達色、上限 0 でのレイアウト、
トレイ通知の実発行（再通知の抑止・月替りや上限額変更での再解禁を含む）はユーザーの実機で確認済み。
ライトテーマでの見え方も 2026-07-31 に確認済み。

---

## 4. データモデル

**ハイブリッド方式**: 本文はプレーンテキストファイル、それ以外は SQLite。

```
%APPDATA%\JpScratch\
├─ settings.json          設定（テーマ・ホットキー・校正・上限額 等）
├─ pricing.json           モデル単価テーブル
├─ credentials.dat        DPAPI 暗号化された API キー
├─ app.db                 SQLite（履歴・ログ・学習データ）
└─ tabs\
   ├─ {tab_id}.txt        タブ本文（UTF-8, BOM なし）
   └─ trash\
      └─ {tab_id}.txt     閉じたタブ（30 日で自動削除）
```

### 4.1 SQLite スキーマ

```sql
-- タブのメタ情報（本文は tabs\{id}.txt）
CREATE TABLE tabs (
    id            TEXT PRIMARY KEY,       -- GUID
    title         TEXT NOT NULL,
    is_auto_title INTEGER NOT NULL DEFAULT 1,
    sort_order    INTEGER NOT NULL,
    is_active     INTEGER NOT NULL DEFAULT 0,
    caret_offset  INTEGER NOT NULL DEFAULT 0,
    created_at    TEXT NOT NULL,
    updated_at    TEXT NOT NULL,
    deleted_at    TEXT                    -- NULL でなければゴミ箱
);

-- API 呼び出しログ（課金の唯一の情報源）
CREATE TABLE api_calls (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    called_at      TEXT    NOT NULL,       -- ISO8601 ローカル
    trigger_type   TEXT    NOT NULL,       -- auto | manual | realternative | styleguide
    model          TEXT    NOT NULL,
    prompt_tokens  INTEGER NOT NULL,
    output_tokens  INTEGER NOT NULL,
    usd_cost       TEXT    NOT NULL,       -- decimal を文字列で保持
    usd_jpy_rate   REAL,
    rate_date      TEXT,
    jpy_cost       TEXT,
    duration_ms    INTEGER NOT NULL,
    status         TEXT    NOT NULL,       -- ok | error | timeout
    error_message  TEXT,
    suggestion_cnt INTEGER NOT NULL DEFAULT 0,
    discarded_cnt  INTEGER NOT NULL DEFAULT 0  -- 位置解決に失敗して捨てた件数
);
CREATE INDEX idx_api_calls_at ON api_calls(called_at);

-- 保持期限を過ぎた api_calls の明細を圧縮して置き換える日次サマリ（3.6.2）
-- 粒度にレートまで含めるので、サマリ1行を「同じレートの call_cnt 件」として
-- 明細行とまったく同じ規約で集計へ流し込める
CREATE TABLE api_call_daily (
    day            TEXT    NOT NULL,       -- ローカル日 yyyy-MM-dd
    trigger_type   TEXT    NOT NULL,
    model          TEXT    NOT NULL,
    status         TEXT    NOT NULL,
    usd_jpy_rate   REAL,
    rate_date      TEXT,
    call_cnt       INTEGER NOT NULL,
    prompt_tokens  INTEGER NOT NULL,
    output_tokens  INTEGER NOT NULL,
    usd_cost       TEXT    NOT NULL,       -- decimal を文字列で保持
    jpy_cost       TEXT,
    suggestion_cnt INTEGER NOT NULL DEFAULT 0,
    discarded_cnt  INTEGER NOT NULL DEFAULT 0
);
CREATE INDEX idx_api_call_daily_day ON api_call_daily(day);

-- リアクション履歴（学習の材料）
CREATE TABLE reactions (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    reacted_at     TEXT NOT NULL,
    api_call_id    INTEGER REFERENCES api_calls(id),
    tab_id         TEXT,
    original       TEXT NOT NULL,
    suggestion     TEXT NOT NULL,
    left_context   TEXT,
    right_context  TEXT,
    reaction       TEXT NOT NULL,          -- accept | reject | reject_with_reason
    user_reason    TEXT,                   -- 理由つき拒否のときのみ
    used_in_prompt INTEGER NOT NULL DEFAULT 0
);
CREATE INDEX idx_reactions_at  ON reactions(reacted_at);
CREATE INDEX idx_reactions_rxn ON reactions(reaction);

-- 自動生成スタイルガイド（履歴として世代を残す）
CREATE TABLE style_guides (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    generated_at      TEXT    NOT NULL,
    content           TEXT    NOT NULL,
    source_reactions  INTEGER NOT NULL,    -- 生成に使ったリアクション件数
    is_active         INTEGER NOT NULL DEFAULT 0,
    is_user_edited    INTEGER NOT NULL DEFAULT 0
);

-- 為替レートキャッシュ
CREATE TABLE fx_rates (
    rate_date  TEXT PRIMARY KEY,           -- Frankfurter が返した基準日 YYYY-MM-DD
    usd_jpy    REAL NOT NULL,
    fetched_at TEXT NOT NULL
);

-- アプリ内部の取得試行などを保持するDB v3メタデータ
CREATE TABLE app_metadata (
    key   TEXT PRIMARY KEY,
    value TEXT NOT NULL
);
```

> 全タブ横断検索は、タブ数が現実的に数十件のためファイル走査で十分。FTS5 は導入しない（DB サイズと複雑さに見合わない）。

---

## 5. マイルストーン

段階的に開発し、**各段階の時点で日常利用できる状態**を保つ。

### v1 — 常駐エディタ（P-1 の解決）

これだけで「WSL で日本語が打てない」問題は消える。

- [x] タスクトレイ常駐、ホットキーによる表示/非表示トグル
- [x] 表示位置ルール（3 方式）、サイズ記憶、マルチモニタ・DPI 対応
- [x] フォーカス喪失時の自動非表示 ＋ クリップボードコピー、ピン留め
- [x] 「全文をコピーして隠す」ホットキー ＋ 直前ウィンドウへのフォーカス復帰
- [x] AvalonEdit によるエディタ（行番号・折り返し・不可視文字表示・文字数カウント）
- [x] タブ（追加/閉じる/並べ替え/自動命名/ゴミ箱と復元）
- [x] 自動保存と再起動時の完全復元
- [x] 検索・置換（正規表現含む）＋ 全タブ横断検索
- [x] テーマ（OS 追従 ＋ 手動）、フォント設定、`Ctrl+ホイール` でのサイズ変更
- [x] 設定画面、MSI インストーラー、スタートアップ登録

**完了の判断基準**: 1 週間、Notepad++ を一度も起動せずに済むこと。

#### 実装時の実測値（2026-07-28, Release / framework-dependent）

| 項目 | 目標 | 実測 |
|---|---|---|
| コールドスタート（ウィンドウが使えるまで） | 1.5 秒以下 | **0.63 秒** |
| 常駐時メモリ（トレイ待機・アイドル） | 80 MB 以下 | **23.4 MB** |
| 表示中メモリ（Working Set） | — | 134 MB |
| インストーラーサイズ | 100 MB 以下 | **1.8 MB** |

> 常駐時メモリは、ウィンドウを隠した直後に GC を走らせて `EmptyWorkingSet` でページを OS に返すことで達成している。
> 次に表示するときはページフォールトで戻るが、一日中トレイに居るアプリとしてはこの取引が正しい。

#### v1 で判明した仕様上の追記

- **配布形態**: MSI は既定でフレームワーク依存（1.8 MB、.NET 10 Desktop Runtime が前提）。
  `installer\build.ps1 -SelfContained` でランタイム同梱版（約 70 MB）も作れる。
- **インストール単位**: ユーザー単位（`%LOCALAPPDATA%\Programs\JP Scratch`）。常駐アプリの導入に管理者権限を要求しないため。
- **スタートアップ登録の所有権**: 初期状態は MSI が `HKCU\...\Run` に書き、アプリは**初回起動時にその有無を読んで設定へ取り込む**。
  以降の変更は設定画面が所有する。こうしないと MSI とアプリが登録を奪い合う。
- **WiX のバージョン**: v5 を使う。v6 以降は Open Source Maintenance Fee の EULA 同意が必要で、これは利用者側の判断事項になる。

### v2 — 校正（P-2 の解決）

- [x] API キー管理（DPAPI ＋ 環境変数の検出と確認）
- [x] Gemini クライアント（全文 `text/plain`、リトライ、タイムアウト）
- [x] 段落分割・変更検出・文脈付き送信
- [x] **提案位置の解決アルゴリズム（3.3.5）と `TextAnchor` による追従**
- [x] インライン下線 ＋ 下部パネル、複数箇所の個別選択
- [x] 許可 / 拒否 / 理由つき拒否 / 理由つき別案生成
- [x] デバウンス ＋ 最小送信間隔 ＋ ハッシュ重複抑止
- [x] `usageMetadata` によるトークン数取得と実行直後の暫定USD料金表示
- [x] `pricing.json` の初回生成・読込、モデル別 `decimal` 料金計算、単価ハードコードの除去
- [x] `pricing.json` の設定画面からの編集（モデル別単価。上限額とは別項目）
- [x] `api_calls` へのUSD料金永続ログ（成功・error・timeout、所要時間・トークン・提案件数と応答破棄時の破棄件数更新）
- [x] ステータスバーへの直近・起動後・当日・当月USD/JPY常時表示、上限進捗バーも実装済み
- [x] Frankfurter v2（ECB）連携、失敗を含む日次試行抑止、古いキャッシュfallback、ログごとのレート基準日スナップショット
- [x] DB v3 `app_metadata` と、料金・ログ・為替を含む全オフライン検証
- [x] 課金履歴画面（期間・種別フィルター、明細表示）。実機確認済み（円欠損行の `¥—`、error/timeout
      行のツールチップ、複数レート混在時の期間合計、明細の並び順、ヘッダ合計とステータスバー月表示の
      一致を含む）
- [x] 月額上限の進捗表示・ガード（発火条件5、確認ダイアログ、進捗バー、トレイ通知、設定画面編集）。
      実機確認済み（進捗バーの配色・上限0時のレイアウト・トレイ通知の実発行・再解禁をダークテーマで
      確認し、ライトテーマでの進捗バーも 2026-07-31 に確認）。設定画面の数値欄は往復不変性のある書式へ
      修正済み（`Services/SettingsFieldFormatting.cs`）
- [x] CSV エクスポートと保持期限後の明細圧縮（3.6.2）。`Services/BillingCsvExporter.cs`、
      `Services/ApiLogRetention.cs`、`ApiCallRepository.Compact`、DB v4 `api_call_daily`。
      CSV の書き出しと Excel での文字化けが無いことは 2026-07-31 に実機確認済み。同日の実機確認で
      「設定画面の補足文が右端で切れる」「保持期間を変えても圧縮が走らない（起動時のみ実行していた）」
      の2件が見つかり、いずれも修正済み。**圧縮後の画面表示の実機確認は未実施**
- [ ] **次の WIP: トレイアイコンの状態表示**（3.1.1、通常/校正中/APIエラー/月間上限到達の4状態）。
      月間上限ガードの実装中に未実装であることが判明し、別WIPとして切り出した
- [x] キーボードだけで提案を処理できる導線（`F8` / `Ctrl+.` / `Ctrl+,`）

**先に潰すべきリスクの検証結果**: `PromptValidation/` の独立検証で、3.1 節の誤り例5本と
文体保護例25本を比較した。採用した `full-rewrite-safe` は誤り検出50/50、文体保護125/125で合格し、
文書中の命令らしい文章を実行しない境界も確認済み（2026-07-29）。
本体は検証済みプロンプトと差分実装を同じソースから参照し、`--self-test` で回帰検査する。

### v3 — 学習（P-3 の解決）

- [ ] リアクション履歴の蓄積と few-shot 選定ロジック
- [ ] スタイルガイドの自動生成（閾値到達時、確認ダイアログつき）
- [ ] スタイルガイドの閲覧・編集・世代管理・無効化
- [ ] ユーザー手書きカスタム指示欄
- [ ] プロンプト構成の統合と、コンテキストキャッシュの適用（対応していれば）
- [ ] 学習効果の可視化（拒否率の推移グラフなど）

**完了の判断基準**: 使い始めた頃と比べ、拒否率（特に「文体を勝手に直された」系の拒否）が明確に下がっていること。

---

## 6. 想定外・非対象（v3 まで）

以下は意図的にスコープ外とする。要望が出たら v4 以降で検討する。

- Markdown プレビュー、シンタックスハイライト
- クラウド同期・複数端末での共有
- 言い回し・読みやすさの改善提案（3.3.3 参照）
- 既存 `.txt` ファイルを開いて編集する「メモ帳型」の運用
- Gemini 以外の LLM プロバイダ対応（ただし API クライアントは差し替え可能な形に切っておく）
- 多言語対応（日本語専用）
- 音声入力

---

## 7. リスクと対策

| # | リスク | 影響 | 対策 |
|---|---|---|---|
| R-1 | ~~モデル ID が存在しない~~ / 単価が変わる | 低 | モデル ID と2026-07-29時点の標準単価は確認済み（3.5.4）。`pricing.json` への外出しと更新日表示を実装し、変更へ追従する |
| R-2 | 全文差分から安全な提案を作れない | **高** | 3.3.5 の再適用検査と過剰変更ガード。応答全体の破棄件数を `api_calls.discarded_cnt` に記録する |
| R-3 | 文体保護が効かず、余計な修正が出続ける | **高** | 検証済みの厳格な全文プロンプトを固定し、迷う修正を禁止する。過剰変更ガードに触れた応答は全破棄する |
| R-4 | 自動チェックでトークンを浪費する | 中 | デバウンス・最小送信間隔・ハッシュ重複抑止・月間上限の 4 段構え |
| R-5 | IME 変換中に自動非表示が発火して入力が消える | 中 | 未確定文字列がある間は非表示を抑止する（3.1.3） |
| R-6 | few-shot が肥大して入力トークンが増え続ける | 中 | スタイルガイドへの圧縮（3.4.2）と件数上限 `K` |
| R-7 | グローバルホットキーが他アプリと衝突する | 低 | 登録失敗を検知して通知し、設定画面で再割り当てできるようにする |
| R-8 | 自動保存中のクラッシュで本文を失う | 中 | 一時ファイルに保存してから `File.Replace` で置き換え、既存の本文を守る |

---

## 8. 決定事項の一覧（本書の根拠）

| 項目 | 決定 |
|---|---|
| 技術スタック | C# WPF ＋ AvalonEdit |
| 校正トリガー | 入力停止後の自動（デバウンス） |
| 永続化 | スクラッチパッド型（常時自動保存） |
| 学習方式 | リアクション履歴の few-shot ＋ スタイルガイド自動生成 ＋ 手書きカスタム指示欄 |
| フォーカス喪失時 | 設定で切替。クリップボードコピーの可否も設定項目 |
| 提案 UI | インライン下線 ＋ 下部パネル |
| 料金表示 | 直近 / セッション累計 / 当日・当月累計 / リクエスト単位のログ画面 |
| API キー | DPAPI 暗号化。環境変数があれば使用するか確認 |
| 送信範囲 | 変更段落 ＋ 前後 1 段落を文脈として添付 |
| 課金ガード | 月間上限額での自動停止 ＋ デバウンス・最小送信間隔 |
| 為替 | Frankfurter を 1 日 1 回取得しローカルキャッシュ |
| 校正スコープ | 誤字・脱字・変換ミス・タイポ ＋ 文法エラー ＋ 表記ゆれ・句読点 |
| 提案の分類・モデル理由 | 生成・表示・保存しない。分類・理由生成用の追加 API も呼ばない |
| 検索・置換 | Notepad++ 相当 ＋ 全タブ横断検索 |
| 理由つき拒否 | 蓄積。再提案するかは都度選択式 |
| ホットキー | 表示トグル / 全文コピーして隠す / 校正実行 / 提案の順送り・許可・拒否 |
| 配布 | MSI インストーラー（スタートアップ登録つき） |
| データストア | 本文＝ファイル、履歴＝SQLite |
| 表示位置 | 設定で切替（既定: タスクバー右下） |
| テーマ | OS 追従 ＋ 手動切替 |
| 開発順序 | v1 常駐エディタ → v2 校正 → v3 学習 |
